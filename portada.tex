% ============================================================
%  Portada elegante — Instalación de Arch Linux
%  Requiere dos pasadas de xelatex (remember picture)
% ============================================================

\definecolor{archblue}{HTML}{1793D1}
\definecolor{coverdark}{HTML}{16213E}
\definecolor{covermid}{HTML}{0F3460}

\newgeometry{margin=0pt}
\thispagestyle{empty}
\pagecolor{covermid}

\begin{tikzpicture}[remember picture, overlay]

  %% ── Fondo oscuro en la mitad superior ─────────────────────
  \fill[coverdark, opacity=0.55]
    ([yshift=8cm]current page.south west)
    rectangle (current page.north east);

  %% ── Franja izquierda ───────────────────────────────────────
  \fill[archblue]
    (current page.south west)
    rectangle ([xshift=5.5pt]current page.north west);

  %% ── Franja inferior ────────────────────────────────────────
  \fill[archblue!80!black]
    (current page.south west)
    rectangle ([yshift=2.5cm]current page.south east);

  %% ── Círculos decorativos (fill/draw, sin \node) ───────────
  \fill[archblue!9!coverdark]
    ([xshift=0pt, yshift=-8.5cm]current page.north) circle (4.8cm);
  \draw[archblue!35, line width=0.8pt]
    ([yshift=-8.5cm]current page.north) circle (5.15cm);
  \draw[archblue!14, line width=0.4pt]
    ([yshift=-8.5cm]current page.north) circle (5.9cm);

  %% ── Logo ───────────────────────────────────────────────────
  \node at ([yshift=-8.5cm]current page.north) {%
    \includegraphics[width=5.8cm]{arch_linux.png}%
  };

  %% ── "Instalación de" ───────────────────────────────────────
  \node[
    text=white!80!coverdark,
    font=\fontsize{32}{40}\selectfont\bfseries
  ] at ([yshift=-15.2cm]current page.north) {Instalación de};

  %% ── "Arch Linux" ───────────────────────────────────────────
  \node[
    text=archblue,
    font=\fontsize{54}{64}\selectfont\bfseries
  ] at ([yshift=-18.2cm]current page.north) {Arch Linux};

  %% ── Línea decorativa ───────────────────────────────────────
  \draw[archblue!50, line width=0.6pt]
    ([yshift=-20.2cm, xshift=-5.5cm]current page.north)
    -- ([yshift=-20.2cm, xshift=5.5cm]current page.north);

  %% ── Subtítulo ──────────────────────────────────────────────
  \node[
    text=white!55!coverdark,
    font=\large\itshape
  ] at ([yshift=-21.5cm]current page.north)
    {Guía de instalación paso a paso};

  %% ── Autor y período ────────────────────────────────────────
  \node[text=white, font=\normalsize\bfseries]
    at ([yshift=1.5cm]current page.south)
    {Félix J. Marcelo Wirnitzer};

  \node[text=white!65!black, font=\small]
    at ([yshift=0.75cm]current page.south)
    {Curso \periodo};

\end{tikzpicture}

\null\vfill
\newpage
\restoregeometry
\pagecolor{white}
\color{black}
